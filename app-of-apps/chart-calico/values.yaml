tigera-operator:
  installCRDs: true
  installation:
    enabled: true
    kubernetesProvider: "EKS"
  kubernetesServiceEndpoint:
    host: "my-cluster.eks.us-east-1.amazonaws.com"
    port: "6443"  

  # apiServer:
  #   enabled: true
  #   namespace: default

    # calicoNetwork:
    #   # Explicitly enable VPC-CNI integration
    #   vpcCni:
    #     enabled: true
    #   # Empty ipPools to ensure Calico doesn't try to manage IP allocation
    #   ipPools: []